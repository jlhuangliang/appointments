import { MockRouterLinkDirective } from './router-link.directive';

describe('MockRouterLinkDirective', () => {
  it('should create an instance', () => {
    const directive = new MockRouterLinkDirective();
    expect(directive).toBeTruthy();
  });
});
